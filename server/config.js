'use strict';

const config = {
  database: 'soloproject',
  username: 'lucaserlacher',
  password: '',
  config: {
    host: 'localhost',
    dialect: 'postgres',
    port: '5432'
  }
};

module.exports = config;